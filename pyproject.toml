[project]
name = "sts_rl"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["numpy", "gymnasium", "torch==2.13.0"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
line-length = 100

[tool.black]
line-length = 100
